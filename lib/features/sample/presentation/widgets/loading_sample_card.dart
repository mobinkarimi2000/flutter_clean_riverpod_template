import 'package:flutter/material.dart';

class LoadingSampleCard extends StatefulWidget {
  const LoadingSampleCard({
    super.key,
  });
  @override
  State<LoadingSampleCard> createState() => _LoadingSampleCardState();
}

class _LoadingSampleCardState extends State<LoadingSampleCard> {
  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
